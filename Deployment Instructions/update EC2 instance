#Not Tested yet

SSH into EC2 instance using hatim.pem

>cd /home/ubuntu/ltei-front/lovetoeatit-front

check which branch its on. git branch -a
if on master just do

>sudo su

>git pull origin

>grunt build

>grunt webrestart